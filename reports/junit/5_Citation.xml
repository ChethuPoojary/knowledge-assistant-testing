<?xml version="1.0" encoding="utf-8"?><testsuites name="pytest tests"><testsuite name="pytest" errors="0" failures="10" skipped="0" tests="10" time="31.769" timestamp="2026-06-14T21:12:35.310172+05:30" hostname="DESKTOP-M1BRJFV"><testcase classname="tests_generated.test_api_from_workbook" name="test_api_case_from_workbook[5_Citation::TC-CIT-001]" time="0.519"><failure message="AssertionError: Expected citation D-001, got: no citations">case = TestCase(sheet='5_Citation', row_number=3, test_case_id='TC-CIT-001', category='Citation', region='Americas', role='Em...r', pass_criteria="Response contains 'USD 25'; citation is D-001 only; not D-002", risk='Medium', testing_layer='Both')

    @allure.epic("Knowledge Assistant")
    @allure.label("owner", AUTHOR)
    @allure.label("tester", AUTHOR)
    @allure.label("automation_engineer", AUTHOR)
    def test_api_case_from_workbook(case):
        allure.dynamic.feature(case.sheet)
        allure.dynamic.story(case.category)
        allure.dynamic.title(f"{case.test_case_id} - {case.prompt}")
        allure.dynamic.description(
            f"Risk: {case.risk}\n"
            f"Region: {case.region}\n"
            f"Role: {case.role}\n"
            f"Expected: {case.expected_behavior}\n"
            f"Pass Criteria: {case.pass_criteria}\n"
            f"Must NOT Do: {case.must_not_do}"
        )
    
        client = KnowledgeAssistantApiClient()
        with allure.step("Prepare API request data"):
            request_details = {
                "test_case_id": case.test_case_id,
                "sheet": case.sheet,
                "region": case.region,
                "role": case.role,
                "prompt": case.prompt,
            }
            allure.attach(json.dumps(request_details, indent=2), "request-data.json", allure.attachment_type.JSON)
    
        with allure.step("Execute API request"):
            if case.prompt.strip().upper().startswith("GET /DOCUMENTS"):
                response = client.documents(case)
            else:
                response = client.query(case)
    
            allure.attach(str(response.status_code), "status-code.txt", allure.attachment_type.TEXT)
            allure.attach(json.dumps(dict(response.headers), indent=2), "response-headers.json", allure.attachment_type.JSON)
            allure.attach(response.text, "response-payload.json", allure.attachment_type.JSON)
    
        with allure.step("Validate API response"):
            try:
                body = response.json()
            except ValueError:
                body = response.text
            evidence = build_evidence(response.status_code, response.text, body)
&gt;           assert_api_case(case, evidence)

tests_generated\test_api_from_workbook.py:54: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

case = TestCase(sheet='5_Citation', row_number=3, test_case_id='TC-CIT-001', category='Citation', region='Americas', role='Em...r', pass_criteria="Response contains 'USD 25'; citation is D-001 only; not D-002", risk='Medium', testing_layer='Both')
evidence = ApiEvidence(answer='I do not have an approved document covering that, so I cannot help with it. Please check with the ...Please check with the relevant team.","citations":[],"user":{"region":"Americas","role":"Employee"}}', status_code=200)

    def assert_api_case(case: TestCase, evidence: ApiEvidence) -&gt; None:
        if case.is_spec_gap:
            assert evidence.status_code &lt; 500, "Spec-gap case should not crash the service."
            return
    
        text = f"{case.expected_behavior} {case.pass_criteria}"
        must_not = case.must_not_do
    
        if _expects_client_error(case):
            assert evidence.status_code in {200, 400, 422}, f"Expected graceful validation, got HTTP {evidence.status_code}"
        else:
            assert evidence.status_code == 200, f"Expected HTTP 200, got {evidence.status_code}"
    
        expects_refusal = _expects_refusal(text)
        expected_docs = set() if expects_refusal else _expected_documents(text)
        forbidden_docs = _forbidden_documents(must_not, case.pass_criteria)
        expected_terms = _expected_terms(text)
        forbidden_terms = _forbidden_terms(must_not)
    
        combined = f"{evidence.answer} {evidence.citations_text}"
    
        if expects_refusal:
            assert _contains_refusal(evidence.answer) or not evidence.answer.strip(), (
                f"Expected refusal language, got answer: {evidence.answer!r}"
            )
    
        for doc_id in expected_docs:
&gt;           assert doc_id in evidence.citations_text, f"Expected citation {doc_id}, got: {evidence.citations_text or 'no citations'}"
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E           AssertionError: Expected citation D-001, got: no citations

framework\assertions.py:77: AssertionError</failure></testcase><testcase classname="tests_generated.test_api_from_workbook" name="test_api_case_from_workbook[5_Citation::TC-CIT-002]" time="0.475"><failure message="AssertionError: Expected citation D-002, got: no citations">case = TestCase(sheet='5_Citation', row_number=4, test_case_id='TC-CIT-002', category='Citation', region='EMEA', role='Employ...r', pass_criteria="Response contains 'EUR 30'; citation is D-002 only; not D-001", risk='Medium', testing_layer='Both')

    @allure.epic("Knowledge Assistant")
    @allure.label("owner", AUTHOR)
    @allure.label("tester", AUTHOR)
    @allure.label("automation_engineer", AUTHOR)
    def test_api_case_from_workbook(case):
        allure.dynamic.feature(case.sheet)
        allure.dynamic.story(case.category)
        allure.dynamic.title(f"{case.test_case_id} - {case.prompt}")
        allure.dynamic.description(
            f"Risk: {case.risk}\n"
            f"Region: {case.region}\n"
            f"Role: {case.role}\n"
            f"Expected: {case.expected_behavior}\n"
            f"Pass Criteria: {case.pass_criteria}\n"
            f"Must NOT Do: {case.must_not_do}"
        )
    
        client = KnowledgeAssistantApiClient()
        with allure.step("Prepare API request data"):
            request_details = {
                "test_case_id": case.test_case_id,
                "sheet": case.sheet,
                "region": case.region,
                "role": case.role,
                "prompt": case.prompt,
            }
            allure.attach(json.dumps(request_details, indent=2), "request-data.json", allure.attachment_type.JSON)
    
        with allure.step("Execute API request"):
            if case.prompt.strip().upper().startswith("GET /DOCUMENTS"):
                response = client.documents(case)
            else:
                response = client.query(case)
    
            allure.attach(str(response.status_code), "status-code.txt", allure.attachment_type.TEXT)
            allure.attach(json.dumps(dict(response.headers), indent=2), "response-headers.json", allure.attachment_type.JSON)
            allure.attach(response.text, "response-payload.json", allure.attachment_type.JSON)
    
        with allure.step("Validate API response"):
            try:
                body = response.json()
            except ValueError:
                body = response.text
            evidence = build_evidence(response.status_code, response.text, body)
&gt;           assert_api_case(case, evidence)

tests_generated\test_api_from_workbook.py:54: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

case = TestCase(sheet='5_Citation', row_number=4, test_case_id='TC-CIT-002', category='Citation', region='EMEA', role='Employ...r', pass_criteria="Response contains 'EUR 30'; citation is D-002 only; not D-001", risk='Medium', testing_layer='Both')
evidence = ApiEvidence(answer='I do not have an approved document covering that, so I cannot help with it. Please check with the ...Please check with the relevant team.","citations":[],"user":{"region":"Americas","role":"Employee"}}', status_code=200)

    def assert_api_case(case: TestCase, evidence: ApiEvidence) -&gt; None:
        if case.is_spec_gap:
            assert evidence.status_code &lt; 500, "Spec-gap case should not crash the service."
            return
    
        text = f"{case.expected_behavior} {case.pass_criteria}"
        must_not = case.must_not_do
    
        if _expects_client_error(case):
            assert evidence.status_code in {200, 400, 422}, f"Expected graceful validation, got HTTP {evidence.status_code}"
        else:
            assert evidence.status_code == 200, f"Expected HTTP 200, got {evidence.status_code}"
    
        expects_refusal = _expects_refusal(text)
        expected_docs = set() if expects_refusal else _expected_documents(text)
        forbidden_docs = _forbidden_documents(must_not, case.pass_criteria)
        expected_terms = _expected_terms(text)
        forbidden_terms = _forbidden_terms(must_not)
    
        combined = f"{evidence.answer} {evidence.citations_text}"
    
        if expects_refusal:
            assert _contains_refusal(evidence.answer) or not evidence.answer.strip(), (
                f"Expected refusal language, got answer: {evidence.answer!r}"
            )
    
        for doc_id in expected_docs:
&gt;           assert doc_id in evidence.citations_text, f"Expected citation {doc_id}, got: {evidence.citations_text or 'no citations'}"
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E           AssertionError: Expected citation D-002, got: no citations

framework\assertions.py:77: AssertionError</failure></testcase><testcase classname="tests_generated.test_api_from_workbook" name="test_api_case_from_workbook[5_Citation::TC-CIT-003]" time="0.503"><failure message="AssertionError: Expected citation D-004, got: D-001">case = TestCase(sheet='5_Citation', row_number=5, test_case_id='TC-CIT-003', category='Citation', region='Americas', role='Em...itation', pass_criteria='Both D-001 and D-004 cited; correct figures in response', risk='Medium', testing_layer='Both')

    @allure.epic("Knowledge Assistant")
    @allure.label("owner", AUTHOR)
    @allure.label("tester", AUTHOR)
    @allure.label("automation_engineer", AUTHOR)
    def test_api_case_from_workbook(case):
        allure.dynamic.feature(case.sheet)
        allure.dynamic.story(case.category)
        allure.dynamic.title(f"{case.test_case_id} - {case.prompt}")
        allure.dynamic.description(
            f"Risk: {case.risk}\n"
            f"Region: {case.region}\n"
            f"Role: {case.role}\n"
            f"Expected: {case.expected_behavior}\n"
            f"Pass Criteria: {case.pass_criteria}\n"
            f"Must NOT Do: {case.must_not_do}"
        )
    
        client = KnowledgeAssistantApiClient()
        with allure.step("Prepare API request data"):
            request_details = {
                "test_case_id": case.test_case_id,
                "sheet": case.sheet,
                "region": case.region,
                "role": case.role,
                "prompt": case.prompt,
            }
            allure.attach(json.dumps(request_details, indent=2), "request-data.json", allure.attachment_type.JSON)
    
        with allure.step("Execute API request"):
            if case.prompt.strip().upper().startswith("GET /DOCUMENTS"):
                response = client.documents(case)
            else:
                response = client.query(case)
    
            allure.attach(str(response.status_code), "status-code.txt", allure.attachment_type.TEXT)
            allure.attach(json.dumps(dict(response.headers), indent=2), "response-headers.json", allure.attachment_type.JSON)
            allure.attach(response.text, "response-payload.json", allure.attachment_type.JSON)
    
        with allure.step("Validate API response"):
            try:
                body = response.json()
            except ValueError:
                body = response.text
            evidence = build_evidence(response.status_code, response.text, body)
&gt;           assert_api_case(case, evidence)

tests_generated\test_api_from_workbook.py:54: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

case = TestCase(sheet='5_Citation', row_number=5, test_case_id='TC-CIT-003', category='Citation', region='Americas', role='Em...itation', pass_criteria='Both D-001 and D-004 cited; correct figures in response', risk='Medium', testing_layer='Both')
evidence = ApiEvidence(answer='For Americas travel, the daily meal allowance is USD 75; receipts are required above USD 25.', cit...ts are required above USD 25.","citations":["D-001"],"user":{"region":"Americas","role":"Employee"}}', status_code=200)

    def assert_api_case(case: TestCase, evidence: ApiEvidence) -&gt; None:
        if case.is_spec_gap:
            assert evidence.status_code &lt; 500, "Spec-gap case should not crash the service."
            return
    
        text = f"{case.expected_behavior} {case.pass_criteria}"
        must_not = case.must_not_do
    
        if _expects_client_error(case):
            assert evidence.status_code in {200, 400, 422}, f"Expected graceful validation, got HTTP {evidence.status_code}"
        else:
            assert evidence.status_code == 200, f"Expected HTTP 200, got {evidence.status_code}"
    
        expects_refusal = _expects_refusal(text)
        expected_docs = set() if expects_refusal else _expected_documents(text)
        forbidden_docs = _forbidden_documents(must_not, case.pass_criteria)
        expected_terms = _expected_terms(text)
        forbidden_terms = _forbidden_terms(must_not)
    
        combined = f"{evidence.answer} {evidence.citations_text}"
    
        if expects_refusal:
            assert _contains_refusal(evidence.answer) or not evidence.answer.strip(), (
                f"Expected refusal language, got answer: {evidence.answer!r}"
            )
    
        for doc_id in expected_docs:
&gt;           assert doc_id in evidence.citations_text, f"Expected citation {doc_id}, got: {evidence.citations_text or 'no citations'}"
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E           AssertionError: Expected citation D-004, got: D-001

framework\assertions.py:77: AssertionError</failure></testcase><testcase classname="tests_generated.test_api_from_workbook" name="test_api_case_from_workbook[5_Citation::TC-CIT-004]" time="0.623"><failure message="AssertionError: Forbidden document D-001 appeared in answer/citations.">case = TestCase(sheet='5_Citation', row_number=6, test_case_id='TC-CIT-004', category='Citation', region='Americas', role='Em...cinate a citation', pass_criteria='D-001 cited; no other doc cited for this claim', risk='Medium', testing_layer='API')

    @allure.epic("Knowledge Assistant")
    @allure.label("owner", AUTHOR)
    @allure.label("tester", AUTHOR)
    @allure.label("automation_engineer", AUTHOR)
    def test_api_case_from_workbook(case):
        allure.dynamic.feature(case.sheet)
        allure.dynamic.story(case.category)
        allure.dynamic.title(f"{case.test_case_id} - {case.prompt}")
        allure.dynamic.description(
            f"Risk: {case.risk}\n"
            f"Region: {case.region}\n"
            f"Role: {case.role}\n"
            f"Expected: {case.expected_behavior}\n"
            f"Pass Criteria: {case.pass_criteria}\n"
            f"Must NOT Do: {case.must_not_do}"
        )
    
        client = KnowledgeAssistantApiClient()
        with allure.step("Prepare API request data"):
            request_details = {
                "test_case_id": case.test_case_id,
                "sheet": case.sheet,
                "region": case.region,
                "role": case.role,
                "prompt": case.prompt,
            }
            allure.attach(json.dumps(request_details, indent=2), "request-data.json", allure.attachment_type.JSON)
    
        with allure.step("Execute API request"):
            if case.prompt.strip().upper().startswith("GET /DOCUMENTS"):
                response = client.documents(case)
            else:
                response = client.query(case)
    
            allure.attach(str(response.status_code), "status-code.txt", allure.attachment_type.TEXT)
            allure.attach(json.dumps(dict(response.headers), indent=2), "response-headers.json", allure.attachment_type.JSON)
            allure.attach(response.text, "response-payload.json", allure.attachment_type.JSON)
    
        with allure.step("Validate API response"):
            try:
                body = response.json()
            except ValueError:
                body = response.text
            evidence = build_evidence(response.status_code, response.text, body)
&gt;           assert_api_case(case, evidence)

tests_generated\test_api_from_workbook.py:54: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

case = TestCase(sheet='5_Citation', row_number=6, test_case_id='TC-CIT-004', category='Citation', region='Americas', role='Em...cinate a citation', pass_criteria='D-001 cited; no other doc cited for this claim', risk='Medium', testing_layer='API')
evidence = ApiEvidence(answer='For Americas travel, the daily meal allowance is USD 75; receipts are required above USD 25.', cit...ts are required above USD 25.","citations":["D-001"],"user":{"region":"Americas","role":"Employee"}}', status_code=200)

    def assert_api_case(case: TestCase, evidence: ApiEvidence) -&gt; None:
        if case.is_spec_gap:
            assert evidence.status_code &lt; 500, "Spec-gap case should not crash the service."
            return
    
        text = f"{case.expected_behavior} {case.pass_criteria}"
        must_not = case.must_not_do
    
        if _expects_client_error(case):
            assert evidence.status_code in {200, 400, 422}, f"Expected graceful validation, got HTTP {evidence.status_code}"
        else:
            assert evidence.status_code == 200, f"Expected HTTP 200, got {evidence.status_code}"
    
        expects_refusal = _expects_refusal(text)
        expected_docs = set() if expects_refusal else _expected_documents(text)
        forbidden_docs = _forbidden_documents(must_not, case.pass_criteria)
        expected_terms = _expected_terms(text)
        forbidden_terms = _forbidden_terms(must_not)
    
        combined = f"{evidence.answer} {evidence.citations_text}"
    
        if expects_refusal:
            assert _contains_refusal(evidence.answer) or not evidence.answer.strip(), (
                f"Expected refusal language, got answer: {evidence.answer!r}"
            )
    
        for doc_id in expected_docs:
            assert doc_id in evidence.citations_text, f"Expected citation {doc_id}, got: {evidence.citations_text or 'no citations'}"
    
        for doc_id in forbidden_docs:
&gt;           assert doc_id not in combined, f"Forbidden document {doc_id} appeared in answer/citations."
                   ^^^^^^^^^^^^^^^^^^^^^^
E           AssertionError: Forbidden document D-001 appeared in answer/citations.

framework\assertions.py:80: AssertionError</failure></testcase><testcase classname="tests_generated.test_api_from_workbook" name="test_api_case_from_workbook[5_Citation::TC-CIT-005]" time="0.561"><failure message="AssertionError: Expected citation D-008, got: no citations">case = TestCase(sheet='5_Citation', row_number=7, test_case_id='TC-CIT-005', category='Citation', region='Americas', role='Fi...s_criteria="Response contains 'VP'; D-008 cited; 'CFO' not stated for this amount", risk='Medium', testing_layer='API')

    @allure.epic("Knowledge Assistant")
    @allure.label("owner", AUTHOR)
    @allure.label("tester", AUTHOR)
    @allure.label("automation_engineer", AUTHOR)
    def test_api_case_from_workbook(case):
        allure.dynamic.feature(case.sheet)
        allure.dynamic.story(case.category)
        allure.dynamic.title(f"{case.test_case_id} - {case.prompt}")
        allure.dynamic.description(
            f"Risk: {case.risk}\n"
            f"Region: {case.region}\n"
            f"Role: {case.role}\n"
            f"Expected: {case.expected_behavior}\n"
            f"Pass Criteria: {case.pass_criteria}\n"
            f"Must NOT Do: {case.must_not_do}"
        )
    
        client = KnowledgeAssistantApiClient()
        with allure.step("Prepare API request data"):
            request_details = {
                "test_case_id": case.test_case_id,
                "sheet": case.sheet,
                "region": case.region,
                "role": case.role,
                "prompt": case.prompt,
            }
            allure.attach(json.dumps(request_details, indent=2), "request-data.json", allure.attachment_type.JSON)
    
        with allure.step("Execute API request"):
            if case.prompt.strip().upper().startswith("GET /DOCUMENTS"):
                response = client.documents(case)
            else:
                response = client.query(case)
    
            allure.attach(str(response.status_code), "status-code.txt", allure.attachment_type.TEXT)
            allure.attach(json.dumps(dict(response.headers), indent=2), "response-headers.json", allure.attachment_type.JSON)
            allure.attach(response.text, "response-payload.json", allure.attachment_type.JSON)
    
        with allure.step("Validate API response"):
            try:
                body = response.json()
            except ValueError:
                body = response.text
            evidence = build_evidence(response.status_code, response.text, body)
&gt;           assert_api_case(case, evidence)

tests_generated\test_api_from_workbook.py:54: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

case = TestCase(sheet='5_Citation', row_number=7, test_case_id='TC-CIT-005', category='Citation', region='Americas', role='Fi...s_criteria="Response contains 'VP'; D-008 cited; 'CFO' not stated for this amount", risk='Medium', testing_layer='API')
evidence = ApiEvidence(answer='I do not have an approved document available to you on that topic.', citations_text='', response_t...ment available to you on that topic.","citations":[],"user":{"region":"Americas","role":"Employee"}}', status_code=200)

    def assert_api_case(case: TestCase, evidence: ApiEvidence) -&gt; None:
        if case.is_spec_gap:
            assert evidence.status_code &lt; 500, "Spec-gap case should not crash the service."
            return
    
        text = f"{case.expected_behavior} {case.pass_criteria}"
        must_not = case.must_not_do
    
        if _expects_client_error(case):
            assert evidence.status_code in {200, 400, 422}, f"Expected graceful validation, got HTTP {evidence.status_code}"
        else:
            assert evidence.status_code == 200, f"Expected HTTP 200, got {evidence.status_code}"
    
        expects_refusal = _expects_refusal(text)
        expected_docs = set() if expects_refusal else _expected_documents(text)
        forbidden_docs = _forbidden_documents(must_not, case.pass_criteria)
        expected_terms = _expected_terms(text)
        forbidden_terms = _forbidden_terms(must_not)
    
        combined = f"{evidence.answer} {evidence.citations_text}"
    
        if expects_refusal:
            assert _contains_refusal(evidence.answer) or not evidence.answer.strip(), (
                f"Expected refusal language, got answer: {evidence.answer!r}"
            )
    
        for doc_id in expected_docs:
&gt;           assert doc_id in evidence.citations_text, f"Expected citation {doc_id}, got: {evidence.citations_text or 'no citations'}"
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E           AssertionError: Expected citation D-008, got: no citations

framework\assertions.py:77: AssertionError</failure></testcase><testcase classname="tests_generated.test_api_from_workbook" name="test_api_case_from_workbook[5_Citation::TC-CIT-006]" time="0.399"><failure message="AssertionError: Expected citation D-008, got: no citations">case = TestCase(sheet='5_Citation', row_number=8, test_case_id='TC-CIT-006', category='Citation', region='Americas', role='Fi...nly; miss the CFO threshold', pass_criteria="Response contains 'CFO'; D-008 cited", risk='Medium', testing_layer='API')

    @allure.epic("Knowledge Assistant")
    @allure.label("owner", AUTHOR)
    @allure.label("tester", AUTHOR)
    @allure.label("automation_engineer", AUTHOR)
    def test_api_case_from_workbook(case):
        allure.dynamic.feature(case.sheet)
        allure.dynamic.story(case.category)
        allure.dynamic.title(f"{case.test_case_id} - {case.prompt}")
        allure.dynamic.description(
            f"Risk: {case.risk}\n"
            f"Region: {case.region}\n"
            f"Role: {case.role}\n"
            f"Expected: {case.expected_behavior}\n"
            f"Pass Criteria: {case.pass_criteria}\n"
            f"Must NOT Do: {case.must_not_do}"
        )
    
        client = KnowledgeAssistantApiClient()
        with allure.step("Prepare API request data"):
            request_details = {
                "test_case_id": case.test_case_id,
                "sheet": case.sheet,
                "region": case.region,
                "role": case.role,
                "prompt": case.prompt,
            }
            allure.attach(json.dumps(request_details, indent=2), "request-data.json", allure.attachment_type.JSON)
    
        with allure.step("Execute API request"):
            if case.prompt.strip().upper().startswith("GET /DOCUMENTS"):
                response = client.documents(case)
            else:
                response = client.query(case)
    
            allure.attach(str(response.status_code), "status-code.txt", allure.attachment_type.TEXT)
            allure.attach(json.dumps(dict(response.headers), indent=2), "response-headers.json", allure.attachment_type.JSON)
            allure.attach(response.text, "response-payload.json", allure.attachment_type.JSON)
    
        with allure.step("Validate API response"):
            try:
                body = response.json()
            except ValueError:
                body = response.text
            evidence = build_evidence(response.status_code, response.text, body)
&gt;           assert_api_case(case, evidence)

tests_generated\test_api_from_workbook.py:54: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

case = TestCase(sheet='5_Citation', row_number=8, test_case_id='TC-CIT-006', category='Citation', region='Americas', role='Fi...nly; miss the CFO threshold', pass_criteria="Response contains 'CFO'; D-008 cited", risk='Medium', testing_layer='API')
evidence = ApiEvidence(answer='I do not have an approved document available to you on that topic.', citations_text='', response_t...ment available to you on that topic.","citations":[],"user":{"region":"Americas","role":"Employee"}}', status_code=200)

    def assert_api_case(case: TestCase, evidence: ApiEvidence) -&gt; None:
        if case.is_spec_gap:
            assert evidence.status_code &lt; 500, "Spec-gap case should not crash the service."
            return
    
        text = f"{case.expected_behavior} {case.pass_criteria}"
        must_not = case.must_not_do
    
        if _expects_client_error(case):
            assert evidence.status_code in {200, 400, 422}, f"Expected graceful validation, got HTTP {evidence.status_code}"
        else:
            assert evidence.status_code == 200, f"Expected HTTP 200, got {evidence.status_code}"
    
        expects_refusal = _expects_refusal(text)
        expected_docs = set() if expects_refusal else _expected_documents(text)
        forbidden_docs = _forbidden_documents(must_not, case.pass_criteria)
        expected_terms = _expected_terms(text)
        forbidden_terms = _forbidden_terms(must_not)
    
        combined = f"{evidence.answer} {evidence.citations_text}"
    
        if expects_refusal:
            assert _contains_refusal(evidence.answer) or not evidence.answer.strip(), (
                f"Expected refusal language, got answer: {evidence.answer!r}"
            )
    
        for doc_id in expected_docs:
&gt;           assert doc_id in evidence.citations_text, f"Expected citation {doc_id}, got: {evidence.citations_text or 'no citations'}"
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E           AssertionError: Expected citation D-008, got: no citations

framework\assertions.py:77: AssertionError</failure></testcase><testcase classname="tests_generated.test_api_from_workbook" name="test_api_case_from_workbook[5_Citation::TC-CIT-007]" time="0.432"><failure message="AssertionError: Expected citation D-004, got: D-005">case = TestCase(sheet='5_Citation', row_number=9, test_case_id='TC-CIT-007', category='Citation', region='APAC', role='Employ...mote work', pass_criteria="D-004 cited; no phantom citation; '3 days' in response", risk='Medium', testing_layer='API')

    @allure.epic("Knowledge Assistant")
    @allure.label("owner", AUTHOR)
    @allure.label("tester", AUTHOR)
    @allure.label("automation_engineer", AUTHOR)
    def test_api_case_from_workbook(case):
        allure.dynamic.feature(case.sheet)
        allure.dynamic.story(case.category)
        allure.dynamic.title(f"{case.test_case_id} - {case.prompt}")
        allure.dynamic.description(
            f"Risk: {case.risk}\n"
            f"Region: {case.region}\n"
            f"Role: {case.role}\n"
            f"Expected: {case.expected_behavior}\n"
            f"Pass Criteria: {case.pass_criteria}\n"
            f"Must NOT Do: {case.must_not_do}"
        )
    
        client = KnowledgeAssistantApiClient()
        with allure.step("Prepare API request data"):
            request_details = {
                "test_case_id": case.test_case_id,
                "sheet": case.sheet,
                "region": case.region,
                "role": case.role,
                "prompt": case.prompt,
            }
            allure.attach(json.dumps(request_details, indent=2), "request-data.json", allure.attachment_type.JSON)
    
        with allure.step("Execute API request"):
            if case.prompt.strip().upper().startswith("GET /DOCUMENTS"):
                response = client.documents(case)
            else:
                response = client.query(case)
    
            allure.attach(str(response.status_code), "status-code.txt", allure.attachment_type.TEXT)
            allure.attach(json.dumps(dict(response.headers), indent=2), "response-headers.json", allure.attachment_type.JSON)
            allure.attach(response.text, "response-payload.json", allure.attachment_type.JSON)
    
        with allure.step("Validate API response"):
            try:
                body = response.json()
            except ValueError:
                body = response.text
            evidence = build_evidence(response.status_code, response.text, body)
&gt;           assert_api_case(case, evidence)

tests_generated\test_api_from_workbook.py:54: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

case = TestCase(sheet='5_Citation', row_number=9, test_case_id='TC-CIT-007', category='Citation', region='APAC', role='Employ...mote work', pass_criteria="D-004 cited; no phantom citation; '3 days' in response", risk='Medium', testing_layer='API')
evidence = ApiEvidence(answer='You can work remotely up to 1 day per week.', citations_text='D-005', response_text='{"answer":"Yo...emotely up to 1 day per week.","citations":["D-005"],"user":{"region":"Americas","role":"Employee"}}', status_code=200)

    def assert_api_case(case: TestCase, evidence: ApiEvidence) -&gt; None:
        if case.is_spec_gap:
            assert evidence.status_code &lt; 500, "Spec-gap case should not crash the service."
            return
    
        text = f"{case.expected_behavior} {case.pass_criteria}"
        must_not = case.must_not_do
    
        if _expects_client_error(case):
            assert evidence.status_code in {200, 400, 422}, f"Expected graceful validation, got HTTP {evidence.status_code}"
        else:
            assert evidence.status_code == 200, f"Expected HTTP 200, got {evidence.status_code}"
    
        expects_refusal = _expects_refusal(text)
        expected_docs = set() if expects_refusal else _expected_documents(text)
        forbidden_docs = _forbidden_documents(must_not, case.pass_criteria)
        expected_terms = _expected_terms(text)
        forbidden_terms = _forbidden_terms(must_not)
    
        combined = f"{evidence.answer} {evidence.citations_text}"
    
        if expects_refusal:
            assert _contains_refusal(evidence.answer) or not evidence.answer.strip(), (
                f"Expected refusal language, got answer: {evidence.answer!r}"
            )
    
        for doc_id in expected_docs:
&gt;           assert doc_id in evidence.citations_text, f"Expected citation {doc_id}, got: {evidence.citations_text or 'no citations'}"
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E           AssertionError: Expected citation D-004, got: D-005

framework\assertions.py:77: AssertionError</failure></testcase><testcase classname="tests_generated.test_ui_from_workbook" name="test_ui_case_from_workbook[5_Citation::TC-CIT-001]" time="9.013"><failure message="Failed: Expected answer to contain 'USD 25', got: 'Knowledge Assistant\nRegion\nAmericas\nEMEA\nAPAC\nRole\nEmployee\nEngineering\nFinance\nManager\nAsk\nI do not have an approved document covering that, so I cannot help with it. Please check with the relevant team.\nDOCUMENTS VISIBLE TO YOU\nTravel and Expense Policy (Americas)\nD-001 · Approved\nRemote Work Policy\nD-004 · Approved'">case = TestCase(sheet='5_Citation', row_number=3, test_case_id='TC-CIT-001', category='Citation', region='Americas', role='Em...r', pass_criteria="Response contains 'USD 25'; citation is D-001 only; not D-002", risk='Medium', testing_layer='Both')
screenshot_dir = WindowsPath('C:/Users/sam/Knowledge-Assistant-Test/reports/screenshots')

    @allure.epic("Knowledge Assistant")
    @allure.label("owner", AUTHOR)
    @allure.label("tester", AUTHOR)
    @allure.label("automation_engineer", AUTHOR)
    def test_ui_case_from_workbook(case, screenshot_dir):
        allure.dynamic.feature(case.sheet)
        allure.dynamic.story(case.category)
        allure.dynamic.title(f"{case.test_case_id} - UI - {case.prompt}")
    
        with sync_playwright() as playwright:
            browser = playwright.chromium.launch(headless=True)
            page = browser.new_page(viewport={"width": 1440, "height": 1000})
            assistant_page = KnowledgeAssistantPage(page, screenshot_dir)
    
            try:
                with allure.step("Navigate to Knowledge Assistant"):
                    assistant_page.open(case)
                with allure.step("Set user region and role"):
                    assistant_page.set_context(case)
                with allure.step("Submit prompt and capture UI response"):
                    page_text = assistant_page.ask_question(case)
                with allure.step("Attach expected and actual results"):
                    _attach_expected_result(case)
                    _attach_actual_result(case, page_text)
                    assistant_page.capture_evidence(case, "expected-actual-evidence")
                with allure.step("Validate UI response text"):
                    evidence = ApiEvidence(
                        answer=page_text,
                        citations_text=page_text,
                        response_text=page_text,
                        status_code=200,
                    )
                    try:
&gt;                       assert_api_case(case, evidence)

tests_generated\test_ui_from_workbook.py:76: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

case = TestCase(sheet='5_Citation', row_number=3, test_case_id='TC-CIT-001', category='Citation', region='Americas', role='Em...r', pass_criteria="Response contains 'USD 25'; citation is D-001 only; not D-002", risk='Medium', testing_layer='Both')
evidence = ApiEvidence(answer='Knowledge Assistant\nRegion\nAmericas\nEMEA\nAPAC\nRole\nEmployee\nEngineering\nFinance\nManager\n...TO YOU\nTravel and Expense Policy (Americas)\nD-001 · Approved\nRemote Work Policy\nD-004 · Approved', status_code=200)

    def assert_api_case(case: TestCase, evidence: ApiEvidence) -&gt; None:
        if case.is_spec_gap:
            assert evidence.status_code &lt; 500, "Spec-gap case should not crash the service."
            return
    
        text = f"{case.expected_behavior} {case.pass_criteria}"
        must_not = case.must_not_do
    
        if _expects_client_error(case):
            assert evidence.status_code in {200, 400, 422}, f"Expected graceful validation, got HTTP {evidence.status_code}"
        else:
            assert evidence.status_code == 200, f"Expected HTTP 200, got {evidence.status_code}"
    
        expects_refusal = _expects_refusal(text)
        expected_docs = set() if expects_refusal else _expected_documents(text)
        forbidden_docs = _forbidden_documents(must_not, case.pass_criteria)
        expected_terms = _expected_terms(text)
        forbidden_terms = _forbidden_terms(must_not)
    
        combined = f"{evidence.answer} {evidence.citations_text}"
    
        if expects_refusal:
            assert _contains_refusal(evidence.answer) or not evidence.answer.strip(), (
                f"Expected refusal language, got answer: {evidence.answer!r}"
            )
    
        for doc_id in expected_docs:
            assert doc_id in evidence.citations_text, f"Expected citation {doc_id}, got: {evidence.citations_text or 'no citations'}"
    
        for doc_id in forbidden_docs:
            assert doc_id not in combined, f"Forbidden document {doc_id} appeared in answer/citations."
    
        for term in expected_terms:
&gt;           assert term.lower() in evidence.answer.lower(), f"Expected answer to contain {term!r}, got: {evidence.answer!r}"
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E           AssertionError: Expected answer to contain 'USD 25', got: 'Knowledge Assistant\nRegion\nAmericas\nEMEA\nAPAC\nRole\nEmployee\nEngineering\nFinance\nManager\nAsk\nI do not have an approved document covering that, so I cannot help with it. Please check with the relevant team.\nDOCUMENTS VISIBLE TO YOU\nTravel and Expense Policy (Americas)\nD-001 · Approved\nRemote Work Policy\nD-004 · Approved'

framework\assertions.py:83: AssertionError

During handling of the above exception, another exception occurred:

case = TestCase(sheet='5_Citation', row_number=3, test_case_id='TC-CIT-001', category='Citation', region='Americas', role='Em...r', pass_criteria="Response contains 'USD 25'; citation is D-001 only; not D-002", risk='Medium', testing_layer='Both')
screenshot_dir = WindowsPath('C:/Users/sam/Knowledge-Assistant-Test/reports/screenshots')

    @allure.epic("Knowledge Assistant")
    @allure.label("owner", AUTHOR)
    @allure.label("tester", AUTHOR)
    @allure.label("automation_engineer", AUTHOR)
    def test_ui_case_from_workbook(case, screenshot_dir):
        allure.dynamic.feature(case.sheet)
        allure.dynamic.story(case.category)
        allure.dynamic.title(f"{case.test_case_id} - UI - {case.prompt}")
    
        with sync_playwright() as playwright:
            browser = playwright.chromium.launch(headless=True)
            page = browser.new_page(viewport={"width": 1440, "height": 1000})
            assistant_page = KnowledgeAssistantPage(page, screenshot_dir)
    
            try:
                with allure.step("Navigate to Knowledge Assistant"):
                    assistant_page.open(case)
                with allure.step("Set user region and role"):
                    assistant_page.set_context(case)
                with allure.step("Submit prompt and capture UI response"):
                    page_text = assistant_page.ask_question(case)
                with allure.step("Attach expected and actual results"):
                    _attach_expected_result(case)
                    _attach_actual_result(case, page_text)
                    assistant_page.capture_evidence(case, "expected-actual-evidence")
                with allure.step("Validate UI response text"):
                    evidence = ApiEvidence(
                        answer=page_text,
                        citations_text=page_text,
                        response_text=page_text,
                        status_code=200,
                    )
                    try:
                        assert_api_case(case, evidence)
                        allure.attach(
                            "Validation Result: PASSED",
                            f"{case.test_case_id} - validation-result.txt",
                            allure.attachment_type.TEXT,
                        )
                        assistant_page.capture_evidence(case, "success-state")
                    except AssertionError as error:
                        allure.attach(
                            f"Validation Result: FAILED\n\nError:\n{error}",
                            f"{case.test_case_id} - validation-result.txt",
                            allure.attachment_type.TEXT,
                        )
                        assistant_page.capture_evidence(case, "failure-state")
&gt;                       pytest.fail(str(error), pytrace=True)
E                       Failed: Expected answer to contain 'USD 25', got: 'Knowledge Assistant\nRegion\nAmericas\nEMEA\nAPAC\nRole\nEmployee\nEngineering\nFinance\nManager\nAsk\nI do not have an approved document covering that, so I cannot help with it. Please check with the relevant team.\nDOCUMENTS VISIBLE TO YOU\nTravel and Expense Policy (Americas)\nD-001 · Approved\nRemote Work Policy\nD-004 · Approved'

tests_generated\test_ui_from_workbook.py:90: Failed</failure></testcase><testcase classname="tests_generated.test_ui_from_workbook" name="test_ui_case_from_workbook[5_Citation::TC-CIT-002]" time="9.074"><failure message="Failed: Expected answer to contain 'EUR 30', got: 'Knowledge Assistant\nRegion\nAmericas\nEMEA\nAPAC\nRole\nEmployee\nEngineering\nFinance\nManager\nAsk\nI do not have an approved document covering that, so I cannot help with it. Please check with the relevant team.\nDOCUMENTS VISIBLE TO YOU\nTravel and Expense Policy (EMEA)\nD-002 · Approved\nRemote Work Policy\nD-004 · Approved'">case = TestCase(sheet='5_Citation', row_number=4, test_case_id='TC-CIT-002', category='Citation', region='EMEA', role='Employ...r', pass_criteria="Response contains 'EUR 30'; citation is D-002 only; not D-001", risk='Medium', testing_layer='Both')
screenshot_dir = WindowsPath('C:/Users/sam/Knowledge-Assistant-Test/reports/screenshots')

    @allure.epic("Knowledge Assistant")
    @allure.label("owner", AUTHOR)
    @allure.label("tester", AUTHOR)
    @allure.label("automation_engineer", AUTHOR)
    def test_ui_case_from_workbook(case, screenshot_dir):
        allure.dynamic.feature(case.sheet)
        allure.dynamic.story(case.category)
        allure.dynamic.title(f"{case.test_case_id} - UI - {case.prompt}")
    
        with sync_playwright() as playwright:
            browser = playwright.chromium.launch(headless=True)
            page = browser.new_page(viewport={"width": 1440, "height": 1000})
            assistant_page = KnowledgeAssistantPage(page, screenshot_dir)
    
            try:
                with allure.step("Navigate to Knowledge Assistant"):
                    assistant_page.open(case)
                with allure.step("Set user region and role"):
                    assistant_page.set_context(case)
                with allure.step("Submit prompt and capture UI response"):
                    page_text = assistant_page.ask_question(case)
                with allure.step("Attach expected and actual results"):
                    _attach_expected_result(case)
                    _attach_actual_result(case, page_text)
                    assistant_page.capture_evidence(case, "expected-actual-evidence")
                with allure.step("Validate UI response text"):
                    evidence = ApiEvidence(
                        answer=page_text,
                        citations_text=page_text,
                        response_text=page_text,
                        status_code=200,
                    )
                    try:
&gt;                       assert_api_case(case, evidence)

tests_generated\test_ui_from_workbook.py:76: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

case = TestCase(sheet='5_Citation', row_number=4, test_case_id='TC-CIT-002', category='Citation', region='EMEA', role='Employ...r', pass_criteria="Response contains 'EUR 30'; citation is D-002 only; not D-001", risk='Medium', testing_layer='Both')
evidence = ApiEvidence(answer='Knowledge Assistant\nRegion\nAmericas\nEMEA\nAPAC\nRole\nEmployee\nEngineering\nFinance\nManager\n...BLE TO YOU\nTravel and Expense Policy (EMEA)\nD-002 · Approved\nRemote Work Policy\nD-004 · Approved', status_code=200)

    def assert_api_case(case: TestCase, evidence: ApiEvidence) -&gt; None:
        if case.is_spec_gap:
            assert evidence.status_code &lt; 500, "Spec-gap case should not crash the service."
            return
    
        text = f"{case.expected_behavior} {case.pass_criteria}"
        must_not = case.must_not_do
    
        if _expects_client_error(case):
            assert evidence.status_code in {200, 400, 422}, f"Expected graceful validation, got HTTP {evidence.status_code}"
        else:
            assert evidence.status_code == 200, f"Expected HTTP 200, got {evidence.status_code}"
    
        expects_refusal = _expects_refusal(text)
        expected_docs = set() if expects_refusal else _expected_documents(text)
        forbidden_docs = _forbidden_documents(must_not, case.pass_criteria)
        expected_terms = _expected_terms(text)
        forbidden_terms = _forbidden_terms(must_not)
    
        combined = f"{evidence.answer} {evidence.citations_text}"
    
        if expects_refusal:
            assert _contains_refusal(evidence.answer) or not evidence.answer.strip(), (
                f"Expected refusal language, got answer: {evidence.answer!r}"
            )
    
        for doc_id in expected_docs:
            assert doc_id in evidence.citations_text, f"Expected citation {doc_id}, got: {evidence.citations_text or 'no citations'}"
    
        for doc_id in forbidden_docs:
            assert doc_id not in combined, f"Forbidden document {doc_id} appeared in answer/citations."
    
        for term in expected_terms:
&gt;           assert term.lower() in evidence.answer.lower(), f"Expected answer to contain {term!r}, got: {evidence.answer!r}"
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E           AssertionError: Expected answer to contain 'EUR 30', got: 'Knowledge Assistant\nRegion\nAmericas\nEMEA\nAPAC\nRole\nEmployee\nEngineering\nFinance\nManager\nAsk\nI do not have an approved document covering that, so I cannot help with it. Please check with the relevant team.\nDOCUMENTS VISIBLE TO YOU\nTravel and Expense Policy (EMEA)\nD-002 · Approved\nRemote Work Policy\nD-004 · Approved'

framework\assertions.py:83: AssertionError

During handling of the above exception, another exception occurred:

case = TestCase(sheet='5_Citation', row_number=4, test_case_id='TC-CIT-002', category='Citation', region='EMEA', role='Employ...r', pass_criteria="Response contains 'EUR 30'; citation is D-002 only; not D-001", risk='Medium', testing_layer='Both')
screenshot_dir = WindowsPath('C:/Users/sam/Knowledge-Assistant-Test/reports/screenshots')

    @allure.epic("Knowledge Assistant")
    @allure.label("owner", AUTHOR)
    @allure.label("tester", AUTHOR)
    @allure.label("automation_engineer", AUTHOR)
    def test_ui_case_from_workbook(case, screenshot_dir):
        allure.dynamic.feature(case.sheet)
        allure.dynamic.story(case.category)
        allure.dynamic.title(f"{case.test_case_id} - UI - {case.prompt}")
    
        with sync_playwright() as playwright:
            browser = playwright.chromium.launch(headless=True)
            page = browser.new_page(viewport={"width": 1440, "height": 1000})
            assistant_page = KnowledgeAssistantPage(page, screenshot_dir)
    
            try:
                with allure.step("Navigate to Knowledge Assistant"):
                    assistant_page.open(case)
                with allure.step("Set user region and role"):
                    assistant_page.set_context(case)
                with allure.step("Submit prompt and capture UI response"):
                    page_text = assistant_page.ask_question(case)
                with allure.step("Attach expected and actual results"):
                    _attach_expected_result(case)
                    _attach_actual_result(case, page_text)
                    assistant_page.capture_evidence(case, "expected-actual-evidence")
                with allure.step("Validate UI response text"):
                    evidence = ApiEvidence(
                        answer=page_text,
                        citations_text=page_text,
                        response_text=page_text,
                        status_code=200,
                    )
                    try:
                        assert_api_case(case, evidence)
                        allure.attach(
                            "Validation Result: PASSED",
                            f"{case.test_case_id} - validation-result.txt",
                            allure.attachment_type.TEXT,
                        )
                        assistant_page.capture_evidence(case, "success-state")
                    except AssertionError as error:
                        allure.attach(
                            f"Validation Result: FAILED\n\nError:\n{error}",
                            f"{case.test_case_id} - validation-result.txt",
                            allure.attachment_type.TEXT,
                        )
                        assistant_page.capture_evidence(case, "failure-state")
&gt;                       pytest.fail(str(error), pytrace=True)
E                       Failed: Expected answer to contain 'EUR 30', got: 'Knowledge Assistant\nRegion\nAmericas\nEMEA\nAPAC\nRole\nEmployee\nEngineering\nFinance\nManager\nAsk\nI do not have an approved document covering that, so I cannot help with it. Please check with the relevant team.\nDOCUMENTS VISIBLE TO YOU\nTravel and Expense Policy (EMEA)\nD-002 · Approved\nRemote Work Policy\nD-004 · Approved'

tests_generated\test_ui_from_workbook.py:90: Failed</failure></testcase><testcase classname="tests_generated.test_ui_from_workbook" name="test_ui_case_from_workbook[5_Citation::TC-CIT-003]" time="8.875"><failure message="Failed: Expected answer to contain '3 days', got: 'Knowledge Assistant\nRegion\nAmericas\nEMEA\nAPAC\nRole\nEmployee\nEngineering\nFinance\nManager\nAsk\nFor Americas travel, the daily meal allowance is USD 75; receipts are required above USD 25.\nD-001\nDOCUMENTS VISIBLE TO YOU\nTravel and Expense Policy (Americas)\nD-001 · Approved\nRemote Work Policy\nD-004 · Approved'">case = TestCase(sheet='5_Citation', row_number=5, test_case_id='TC-CIT-003', category='Citation', region='Americas', role='Em...itation', pass_criteria='Both D-001 and D-004 cited; correct figures in response', risk='Medium', testing_layer='Both')
screenshot_dir = WindowsPath('C:/Users/sam/Knowledge-Assistant-Test/reports/screenshots')

    @allure.epic("Knowledge Assistant")
    @allure.label("owner", AUTHOR)
    @allure.label("tester", AUTHOR)
    @allure.label("automation_engineer", AUTHOR)
    def test_ui_case_from_workbook(case, screenshot_dir):
        allure.dynamic.feature(case.sheet)
        allure.dynamic.story(case.category)
        allure.dynamic.title(f"{case.test_case_id} - UI - {case.prompt}")
    
        with sync_playwright() as playwright:
            browser = playwright.chromium.launch(headless=True)
            page = browser.new_page(viewport={"width": 1440, "height": 1000})
            assistant_page = KnowledgeAssistantPage(page, screenshot_dir)
    
            try:
                with allure.step("Navigate to Knowledge Assistant"):
                    assistant_page.open(case)
                with allure.step("Set user region and role"):
                    assistant_page.set_context(case)
                with allure.step("Submit prompt and capture UI response"):
                    page_text = assistant_page.ask_question(case)
                with allure.step("Attach expected and actual results"):
                    _attach_expected_result(case)
                    _attach_actual_result(case, page_text)
                    assistant_page.capture_evidence(case, "expected-actual-evidence")
                with allure.step("Validate UI response text"):
                    evidence = ApiEvidence(
                        answer=page_text,
                        citations_text=page_text,
                        response_text=page_text,
                        status_code=200,
                    )
                    try:
&gt;                       assert_api_case(case, evidence)

tests_generated\test_ui_from_workbook.py:76: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

case = TestCase(sheet='5_Citation', row_number=5, test_case_id='TC-CIT-003', category='Citation', region='Americas', role='Em...itation', pass_criteria='Both D-001 and D-004 cited; correct figures in response', risk='Medium', testing_layer='Both')
evidence = ApiEvidence(answer='Knowledge Assistant\nRegion\nAmericas\nEMEA\nAPAC\nRole\nEmployee\nEngineering\nFinance\nManager\n...TO YOU\nTravel and Expense Policy (Americas)\nD-001 · Approved\nRemote Work Policy\nD-004 · Approved', status_code=200)

    def assert_api_case(case: TestCase, evidence: ApiEvidence) -&gt; None:
        if case.is_spec_gap:
            assert evidence.status_code &lt; 500, "Spec-gap case should not crash the service."
            return
    
        text = f"{case.expected_behavior} {case.pass_criteria}"
        must_not = case.must_not_do
    
        if _expects_client_error(case):
            assert evidence.status_code in {200, 400, 422}, f"Expected graceful validation, got HTTP {evidence.status_code}"
        else:
            assert evidence.status_code == 200, f"Expected HTTP 200, got {evidence.status_code}"
    
        expects_refusal = _expects_refusal(text)
        expected_docs = set() if expects_refusal else _expected_documents(text)
        forbidden_docs = _forbidden_documents(must_not, case.pass_criteria)
        expected_terms = _expected_terms(text)
        forbidden_terms = _forbidden_terms(must_not)
    
        combined = f"{evidence.answer} {evidence.citations_text}"
    
        if expects_refusal:
            assert _contains_refusal(evidence.answer) or not evidence.answer.strip(), (
                f"Expected refusal language, got answer: {evidence.answer!r}"
            )
    
        for doc_id in expected_docs:
            assert doc_id in evidence.citations_text, f"Expected citation {doc_id}, got: {evidence.citations_text or 'no citations'}"
    
        for doc_id in forbidden_docs:
            assert doc_id not in combined, f"Forbidden document {doc_id} appeared in answer/citations."
    
        for term in expected_terms:
&gt;           assert term.lower() in evidence.answer.lower(), f"Expected answer to contain {term!r}, got: {evidence.answer!r}"
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E           AssertionError: Expected answer to contain '3 days', got: 'Knowledge Assistant\nRegion\nAmericas\nEMEA\nAPAC\nRole\nEmployee\nEngineering\nFinance\nManager\nAsk\nFor Americas travel, the daily meal allowance is USD 75; receipts are required above USD 25.\nD-001\nDOCUMENTS VISIBLE TO YOU\nTravel and Expense Policy (Americas)\nD-001 · Approved\nRemote Work Policy\nD-004 · Approved'

framework\assertions.py:83: AssertionError

During handling of the above exception, another exception occurred:

case = TestCase(sheet='5_Citation', row_number=5, test_case_id='TC-CIT-003', category='Citation', region='Americas', role='Em...itation', pass_criteria='Both D-001 and D-004 cited; correct figures in response', risk='Medium', testing_layer='Both')
screenshot_dir = WindowsPath('C:/Users/sam/Knowledge-Assistant-Test/reports/screenshots')

    @allure.epic("Knowledge Assistant")
    @allure.label("owner", AUTHOR)
    @allure.label("tester", AUTHOR)
    @allure.label("automation_engineer", AUTHOR)
    def test_ui_case_from_workbook(case, screenshot_dir):
        allure.dynamic.feature(case.sheet)
        allure.dynamic.story(case.category)
        allure.dynamic.title(f"{case.test_case_id} - UI - {case.prompt}")
    
        with sync_playwright() as playwright:
            browser = playwright.chromium.launch(headless=True)
            page = browser.new_page(viewport={"width": 1440, "height": 1000})
            assistant_page = KnowledgeAssistantPage(page, screenshot_dir)
    
            try:
                with allure.step("Navigate to Knowledge Assistant"):
                    assistant_page.open(case)
                with allure.step("Set user region and role"):
                    assistant_page.set_context(case)
                with allure.step("Submit prompt and capture UI response"):
                    page_text = assistant_page.ask_question(case)
                with allure.step("Attach expected and actual results"):
                    _attach_expected_result(case)
                    _attach_actual_result(case, page_text)
                    assistant_page.capture_evidence(case, "expected-actual-evidence")
                with allure.step("Validate UI response text"):
                    evidence = ApiEvidence(
                        answer=page_text,
                        citations_text=page_text,
                        response_text=page_text,
                        status_code=200,
                    )
                    try:
                        assert_api_case(case, evidence)
                        allure.attach(
                            "Validation Result: PASSED",
                            f"{case.test_case_id} - validation-result.txt",
                            allure.attachment_type.TEXT,
                        )
                        assistant_page.capture_evidence(case, "success-state")
                    except AssertionError as error:
                        allure.attach(
                            f"Validation Result: FAILED\n\nError:\n{error}",
                            f"{case.test_case_id} - validation-result.txt",
                            allure.attachment_type.TEXT,
                        )
                        assistant_page.capture_evidence(case, "failure-state")
&gt;                       pytest.fail(str(error), pytrace=True)
E                       Failed: Expected answer to contain '3 days', got: 'Knowledge Assistant\nRegion\nAmericas\nEMEA\nAPAC\nRole\nEmployee\nEngineering\nFinance\nManager\nAsk\nFor Americas travel, the daily meal allowance is USD 75; receipts are required above USD 25.\nD-001\nDOCUMENTS VISIBLE TO YOU\nTravel and Expense Policy (Americas)\nD-001 · Approved\nRemote Work Policy\nD-004 · Approved'

tests_generated\test_ui_from_workbook.py:90: Failed</failure></testcase></testsuite></testsuites>