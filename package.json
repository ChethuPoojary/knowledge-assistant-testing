{
  "name": "knowledge-assistant-testing",
  "version": "1.0.0",
  "private": true,
  "description": "QA deliverables and automation for the Knowledge Assistant assignment.",
  "scripts": {
    "test:ui": "playwright test",
    "test:api": "pytest api-tests -v"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}

